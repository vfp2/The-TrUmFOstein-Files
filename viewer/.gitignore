# dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
next-env.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel/

# typescript
*.tsbuildinfo

# env
.env*.local

# Generated/large data — these are recommitted by the export script and
# don't need to be in git unless you actually want them shipped. For the
# viewer to be deployable straight from a clone, we DO commit graph.json
# and the per-doc bundles. Visual-artifact assets too. Comment-in below
# only if you want to manage them out-of-band.
# /public/graph.json
# /public/docs/
# /public/visual-artifacts/
